--request 페이지
#url과 title값이 공백이면 안됨. url값에 flag라는 문자열이 들어있으면 안되고 url이 file:// 문자열로 시작하면안됨.

--upload 페이지
#파일이 존재하고 제목이 공백이면 안됨

두개다 결국에는 파일을 난독화 해서 mini_database라는 배열에 데이터를 저장함.
request는 url로 upload는 파일로 이미지를 업로드 하는것임.

upload 페이지의 타이틀부분에 xss는 안되는거같음. 진자템플릿 부분에서 xss막아놓은듯

request페이지에서 외부url에 요청을 보낼수있는데 검증하는 코드가 없음.
설명처럼 이쪽으로 뚫는게 정답인듯

urlopen 함수에 취약점이 있는지 확인해보자